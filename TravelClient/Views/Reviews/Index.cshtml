@{
  Layout = "_Layout";
}

@using TravelClient.Models;
@model IEnumerable<TravelClient.Models.Review>

<div class="jumbotron">
    <h2 class="display-4"> Reviews</h2>
</div>

@* <form asp-controller="Reviews" asp-action="get" method="get"> *@
  
  <div class="searchheader">
    <div class="search">
        <h3>Search Reviews</h3>
    </div>
    <div>
    </div>
    <div>
    </div>
    <div>
      @* <form asp-controller="Reviews" asp-action="get" method="get"asp-controller="Reviews" asp-action="get" method="get" class="form-inline my-2 my-lg-0">
          <input name="searchString" class="form-control mr-sm-2" type="search" placeholder="Seattle, Washington" aria-label="Search">
          <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
      </form> *@
    </div>
  </div>
 <br>

<div class='content'>
  @if(Model.Any())
  {
    <table class="table table-striped">
      <tr>
        <th scope="col">City, Country</th>
        <th scope="col">Description</th>
        <th scope="col">Rating</th>
        <th scope="col">Reviewer</th>
      </tr>
    @foreach(Review review in Model)
    {
      <tr>
        <td>@Html.ActionLink($"{review.Destination.City}, {review.Destination.Country}", "Details","Destinations", new { id = review.DestinationId })</td>
        <td>@review.Description</td>
        <td>@review.Rating</td>
        <td>@review.ReviewerName</td>
      </tr>
    }
    </table>
  }
  else
  {
    <h3>No reviews available</h3>
  }
  <p>@Html.ActionLink("Add a review", "Create")</p>